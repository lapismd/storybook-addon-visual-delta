# Visual Delta configuration specification moved

> Compatibility pointer. This file is non-normative.

The canonical page is [Visual Delta configuration](../spec/src/configuration.md). Edit the canonical source instead of this pointer.
