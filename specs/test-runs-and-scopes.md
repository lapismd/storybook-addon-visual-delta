# Visual Delta test runs and scopes specification moved

> Compatibility pointer. This file is non-normative.

The canonical page is [Visual Delta test runs and scopes](../spec/src/test-runs-and-scopes.md). Edit the canonical source instead of this pointer.
